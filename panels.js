import {key,quoteKey,usable,day,today,uid} from './book.js';
export function assetView(u){const {state,book,classOf,scoped,buckets,pie,cash,pct,esc}=u,values=new Map(buckets.map(b=>[b,0])),missing=[],dates=[],assets=state.assets.filter(scoped),fx=state.settings.fx;function add(bucket,value,currency,date){if(currency==='USD'&&!fx){missing.push('美元估值匯率');return;}values.set(bucket,values.get(bucket)+value*(currency==='USD'?fx.rate:1));dates.push(date);}for(const p of book().positions.filter(p=>p.quantity>0)){const q=state.quotes[quoteKey(p)];if(!usable(q)){missing.push(p.symbol+' 股價');continue;}const type=state.entries.find(e=>key(e)===key(p))?.assetType;add(classOf(p)?.bucket||(type==='bond'?'債券':p.market==='TW'?'台股':'美股'),p.quantity*q.close,p.market==='US'?'USD':'TWD',q.date);}for(const a of assets)add(a.bucket,a.amount,a.currency,a.date);for(const a of state.accounts.filter(a=>scoped({account:a.id})))if(!assets.some(x=>x.account===a.id&&x.bucket==='現金'))missing.push(`${a.name} 的現金餘額（沒有可填 0）`);const rows=[...values],total=rows.reduce((s,[,v])=>s+v,0),targets=state.settings.targets,valid=Math.abs(Object.values(targets).reduce((s,v)=>s+v,0)-100)<.01;
return `<section class="paper"><span class="meta">${missing.length?'已知部分資產':'已記錄資產'} · 台幣估值</span><h2 class="amount-title">${cash(total)}</h2>${pie(rows)}${missing.length?`<p class="warning">還缺 ${esc([...new Set(missing)].join('、'))}。補齊前不計算配置調整。</p>`:''}<p class="meta">只含已登錄資產，不含未記錄資產或負債。${fx?`USD/TWD ${fx.rate} · ${esc(fx.date)} 手動匯率。`:''}${dates.some(d=>day(today())-day(d)>7)?'部分估值超過 7 天，請更新。':''}</p></section><section class="panel"><div class="record-top"><h2>離我想要的比例，還差多少？</h2><button class="text-link" data-targets type="button">設定目標</button></div>${!valid?'<p>先選擇配置範例，再改成自己的目標。</p>':missing.length?'<p>請先補齊資料，再查看調整差額。</p>':rows.map(([b,v])=>{const actual=total?v/total*100:0,target=targets[b]||0,adjust=total*target/100-v,alert=Math.abs(actual-target)>state.settings.tolerance;return `<div class="allocation-row${alert?' signal':''}"><div class="record-top"><strong>${b}</strong><span>${pct(actual/100)} / 目標 ${target}%</span></div><p>${adjust>=0?'距目標可增加':'距目標可減少'} ${cash(Math.abs(adjust))}${alert?' · 超過自訂偏差':''}</p></div>`;}).join('')}<p class="meta">這是你設定的比例差額，未計費稅；不是自動買賣建議或收益保證。</p></section><details class="panel"><summary>我的換匯成本筆記</summary><p class="meta">依買入／賣出美元紀錄加權，不扣除股票交易用掉的美元，因此不是帳戶現金餘額。</p>${book().fx.map(f=>`<article class="record"><strong>${esc(u.accountName(f.account))}</strong><p>未配對賣匯的美元 ${cash(f.usd,'USD')} · 平均買匯成本 ${f.usd?(f.cost/f.usd).toFixed(4):'—'}</p><p>累計賣匯損益 ${cash(f.realized)}${fx&&f.usd?` · 以目前參考匯率估算價差 ${cash(f.usd*fx.rate-f.cost)}`:''}</p></article>`).join('')||'<p class="empty">到記帳頁選「換匯」開始記錄。</p>'}</details><section class="panel"><h2>現金與其他資產</h2><p>餘額快照不會隨買賣／換匯自動加減。請定期對帳，勿重複登錄股票。</p>${assets.map(a=>`<article class="record"><div class="record-top"><strong>${esc(a.name)}</strong><span>${cash(a.amount,a.currency)}</span></div><span class="meta">${esc(u.accountName(a.account))} · ${a.bucket} · ${esc(a.date)}</span><button class="text-link" type="button" data-asset="${esc(a.id)}">更新／刪除</button></article>`).join('')||'<p class="empty">尚無現金或其他資產</p>'}<button class="text-link" data-asset-add type="button">＋ 新增資產</button></section>`;}
export function assetDialogContent(u,id=''){const {state,field,esc,problem}=u,a=state.assets.find(x=>x.id===id)||{};return `<form data-form="asset"><input name="id" type="hidden" value="${esc(id)}">${problem}${field('資產名稱','name','text',a.name||'',null,true)}<div class="row">${field('配置類別','bucket','text',a.bucket||'現金',u.buckets.map(x=>[x,x]))}${field('幣別','currency','text',a.currency||'TWD',[['TWD','台幣'],['USD','美元']])}</div><div class="row">${field('目前餘額','amount','number',a.amount??'',null,true)}${field('餘額日期','date','date',a.date||today(),null,true)}</div>${field('帳戶','account','text',a.account||u.account(),state.accounts.map(a=>[a.id,a.name]))}<button class="primary" type="submit">儲存這份餘額</button>${id?`<button type="button" class="text-link" data-delete-asset="${esc(id)}">刪除資產</button>`:''}</form><hr class="divider"><h3>美元估值匯率</h3><p class="meta">填永豐美元即期買入價或你的估值參考；目前手動更新，不會改寫歷史換匯或原幣年化。</p><form data-form="fx-rate"><div class="row">${field('1 USD = TWD','rate','number',state.settings.fx?.rate||'',null,true)}${field('匯率日期','date','date',state.settings.fx?.date||today(),null,true)}</div>${problem}<button class="secondary" type="submit">更新估值匯率</button></form>`;}
export function targetContent(u){const {field,state,buckets,problem}=u;return `<p>範例僅供自行調整，未依你的財務狀況個人化推薦。</p>${field('範例：台股／美股／債券／現金','example','text','',[['','自行設定'],['20,20,40,20','範例 A · 20／20／40／20'],['20,40,30,10','範例 B · 20／40／30／10'],['30,50,15,5','範例 C · 30／50／15／5']])}<form data-form="targets"><div class="row">${buckets.map((b,i)=>field(`${b}目標（%）`,'target'+i,'number',state.settings.targets[b]||0,null,true)).join('')}</div>${field('容許偏差（百分點）','tolerance','number',state.settings.tolerance,null,true)}${problem}<button class="primary" type="submit">套用我的目標</button><p class="meta">合計需 100%，提醒只出現在資產頁。</p></form>`;}
export function settingsContent(u){const {state,field,problem,esc}=u,c=u.connection();return `<details class="panel" open><summary>帳戶與常用券商</summary><form data-form="account"><p>同股分帳算成本；「合併看」只合併畫面。</p>${field('新增帳戶名稱','name','text','',null,true)}${field('預設券商','broker','text',u.defaultBroker(),state.brokers.map(b=>[b.id,b.name]))}${problem}<button class="secondary" type="submit">新增帳戶</button></form><hr class="divider"><form data-form="default-broker">${field('目前帳戶常用券商','broker','text',u.defaultBroker(),state.brokers.map(b=>[b.id,b.name]))}${problem}<button class="secondary" type="submit">儲存預設</button></form><button class="text-link" data-brokers type="button">詳細設定台股／美股費稅 →</button></details><details class="panel"><summary>分類與黃色提醒</summary>${state.settings.categories.map(c=>`<div class="record-top"><span>${esc(c.name)}</span><button class="text-link" data-category="${esc(c.id)}" type="button">修改門檻</button></div>`).join('')}<button class="text-link" data-category="new" type="button">＋ 新增分類</button></details><details class="panel"><summary>備份、換手機與變更紀錄</summary><p>定期匯出；清除網站資料、私密瀏覽或換手機可能遺失帳本。備份不含 Token。</p><div class="actions"><button class="secondary" data-export type="button">匯出備份</button><button class="secondary" data-restore-snapshot type="button">還原匯入前</button></div><div class="field"><label for="backup-file">匯入慢慢記新版備份</label><input id="backup-file" type="file" accept=".json,application/json"></div><p class="meta">新資料格式，不會自動讀取或覆蓋舊版。舊備份請保留。</p><button class="text-link" data-history type="button">查看修改／刪除前內容</button></details><details class="panel"><summary>Tiingo 美股備援（選用）</summary><p>預設關閉。GitHub Pages 直接連線可能受限，可自行部署隨附中繼。不要使用陌生中繼，它會收到 Token。</p><form data-form="tiingo"><label class="radio-line"><input name="enabled" type="checkbox"${c.enabled?' checked':''}>啟用美股行情備援</label>${field('API Token（空白保留已存值）','token','password','')}${field('自有中繼 HTTPS 網址（選填）','relay','url',c.relay||'')}<label class="radio-line"><input name="auto" type="checkbox"${c.auto?' checked':''}>連網自動更新日收盤</label>${problem}<button class="secondary" type="submit">儲存連線設定</button><button class="text-link" data-test-tiingo type="button">測試已儲存的連線</button></form></details><details class="panel"><summary>定期定額記帳預設</summary><p>只預填表單，不會把計畫當已成交。實際成交價與股數仍由你確認。</p><form data-form="plan">${field('市場','market','text','TW',[['TW','台股'],['US','美股']])}${field('常用代號','symbol','text',state.settings.plans?.TW?.symbol||'')}${field('分類','category','text',state.settings.plans?.TW?.category||'',[['','未分類'],...state.settings.categories.map(c=>[c.id,c.name])])}${problem}<button class="secondary" type="submit">儲存市場預設</button></form><div class="actions"><button class="text-link" data-use-plan="TW" type="button">台股預設記一筆</button><button class="text-link" data-use-plan="US" type="button">美股預設記一筆</button></div></details>`;}
export function brokerContent(u,id){const {state,field,problem,esc}=u,old=state.brokers.find(b=>b.id===id),b=old||state.brokers[0];return `${field('選擇券商','broker-picker','text',old?.id||'new',[...state.brokers.map(b=>[b.id,b.name]),['new','＋ 新增券商']])}<form data-form="broker"><input name="id" type="hidden" value="${esc(old?.id||uid())}">${field('券商名稱','name','text',old?.name||'',null,true)}<details class="panel" open><summary>台股手續費與稅率</summary><div class="row">${field('基本費率（%）','tw-rate','number',b.tw.rate*100,null,true)}${field('折數（例：2.8 折）','tw-discount','number',b.tw.discount*10,null,true)}${field('最低費（TWD）','tw-min','number',b.tw.min,null,true)}${field('小數處理','tw-round','text',b.tw.round,[['round','四捨五入'],['floor','捨去'],['ceil','進位']])}${field('股票稅率（%）','tax-stock','number',state.settings.tax.stock*100,null,true)}${field('一般 ETF 稅率（%）','tax-etf','number',state.settings.tax.etf*100,null,true)}${field('合資格當沖（%）','tax-day','number',state.settings.tax.day*100,null,true)}</div><p class="meta">合資格被動債券 ETF 停徵至 2026-12-31；主動／槓桿／反向不適用。稅率全帳本共用；當沖優惠到期後依普通股票試算。請核對交易當時規定。</p></details><details class="panel" open><summary>美股手續費與賣出稅費</summary>${field('主要計費方式','us-mode','text',b.us.mode,[['fixed','每筆固定 USD'],['share','每股 USD'],['percent','成交額百分比']])}<div class="row">${field('費率／金額（百分比填 %）','us-rate','number',b.us.rate*(b.us.mode==='percent'?100:1),null,true)}${[['min','最低基本費'],['extra','另外每筆費'],['perShare','另外每股費'],['sellRate','賣出稅費率（%）'],['sellShare','賣出每股稅費'],['sellMin','賣出稅費最低額']].map(([f,l])=>field(l,'us-'+f,'number',b.us[f]*(f==='sellRate'?100:1),null,true)).join('')}</div><p class="meta">初始 0 不代表必然免稅。可加總每筆／每股費；複雜分項進位或上限請用實付覆寫。金額單位 USD。</p></details>${problem}<button class="primary" type="submit">儲存券商費稅</button><p class="meta">新交易套用，舊交易保留實付快照。</p></form>`;}
export function categoryContent(u,id){const c=u.state.settings.categories.find(x=>x.id===id)||{id:uid(),name:''};return `<form data-form="category"><input name="id" type="hidden" value="${u.esc(c.id)}">${u.field('分類名稱','name','text',c.name,null,true)}<div class="row">${[['profit','獲利報酬率達（%）'],['loss','虧損幅度達（%）'],['rise','今日上漲達（%）'],['fall','今日下跌達（%）']].map(([f,l])=>u.field(l,f,'number',c[f]??'')).join('')}</div><p class="meta">填正數，留空／0 關閉；舊行情不觸發今日漲跌提醒。</p>${u.problem}<button class="primary" type="submit">儲存分類與提醒</button></form>`;}
